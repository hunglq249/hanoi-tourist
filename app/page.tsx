// This file is intentionally empty.
// The home page is served from app/[locale]/page.tsx via next-intl routing.
// The middleware redirects / → vi (default locale, no prefix) and /en → en locale.
export default function Page() {
  return null;
}
